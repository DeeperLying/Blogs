/*
import 'package:meta/meta.dart';

class User { // 用于定义存储内容
  String _name;
  get name => _name;
  User(this._name);
}

enum Action {
  increment
}

ContState reducer(ContState state, action){
  // 匹配Action
  if(action == Action.increment){
  return CountState(state.count+1);
  }
  return state;
}
*/
